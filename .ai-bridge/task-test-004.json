{
  "task_id": "test-004",
  "prompt": "Это тестовая задача watcher'а (test-004) — проверка исправленного механизма запуска процесса (ExitCode теперь должен корректно возвращаться как integer через System.Diagnostics.Process). НИЧЕГО не меняй, не коммить и не пушь. Просто выполни `git status --short` и `git branch --show-current` в текущей директории и выведи результат в стандартный вывод."
}

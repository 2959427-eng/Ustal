{
  "task_id": "commit-audit-2026-09-08",
  "prompt": "Задача: полный git-аудит текущих локальных изменений в C:\\Users\\User\\Desktop\\ustal-foundation (репозиторий 2959427-eng/Ustal) и безопасное сохранение уже готовой мобильной работы в логические коммиты.\n\nЖЁСТКИЕ ОГРАНИЧЕНИЯ (см. также AGENTS.md в корне репозитория — они действуют всегда, не только для этой задачи):\n- НИЧЕГО не откатывать, НЕ делать git reset --hard, НЕ делать git clean, НЕ удалять untracked-файлы.\n- НЕ пушить (git push) — только локальные коммиты. Пуш будет отдельным явным разрешением от пользователя позже.\n- Timeweb networking НЕ трогать. Infra НЕ трогать. Production-секреты НЕ трогать и никуда не выводить.\n- Не откатывай проект к origin/main — текущий локальный HEAD является источником истины.\n\nПОРЯДОК ДЕЙСТВИЙ (строго по шагам, не пропускай и не меняй местами):\n\n1. Сначала покажи текущее состояние, НИЧЕГО не меняя: git status, git diff (staged и unstaged отдельно), git branch --show-current, git remote -v. Выведи это в стандартный вывод целиком, прежде чем переходить к следующему шагу.\n\n2. Разбей накопленные изменения на логические коммиты (НЕ один общий коммит на всё). Уже есть предложенное разбиение на 9 коммитов чуть ниже в этом же файле AI_HANDOFF.md, в разделе \"AI_HANDOFF — 2026-09-08 (Claude Cowork → Codex: закоммитить накопленное в логические коммиты + починил refresh-session flow, нужна проверка)\" — прочитай его перед началом. Ориентируйся на эту структуру (auth foundation, AI-профиль, заказы/лента/мои, экран заказа, голос/фото, notifications/settings/reports/blocks, backend compat fixes, app.json/babel fix, security fixes), но с одним уточнением: refresh-session fix (apps/mobile/src/api/auth.ts — функция refreshSession(), и apps/mobile/app/_layout.tsx) должен идти ОТДЕЛЬНЫМ коммитом, не смешанным с другими security/compat fixes. Если по факту diff'а реальное разбиение файлов немного отличается от предложенного в AI_HANDOFF.md — используй своё разумное суждение, главное чтобы коммиты были логически цельными (одна функциональная область на коммит), а не одним большим блоком.\n\n3. После КАЖДОГО коммита покажи: SHA коммита (полный или короткий) и список файлов, вошедших в него (например git show --stat <sha> или git diff-tree --no-commit-id --name-only -r <sha>).\n\n4. В конце покажи итоговый git status (что осталось незакоммиченным, если что-то осталось осознанно) и список ВСЕХ созданных этой задачей коммитов (git log --oneline с диапазоном, соответствующим этой сессии).\n\n5. Допиши результат (что реально сделано: список коммитов с SHA, что осталось незакоммиченным и почему) в AI_HANDOFF.md отдельным блоком сверху, с датой и подписью 'Codex:', как обычно — не удаляя предыдущие разделы.\n\nЕщё раз: не пушить, не трогать Timeweb/infra, не откатывать и не удалять ничего."
}

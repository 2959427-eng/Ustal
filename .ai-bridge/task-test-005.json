{
  "task_id": "test-005",
  "prompt": "Это тестовая задача watcher'а (test-005) — проверка исправления pid=null в финальном статусе (PID теперь захватывается сразу после запуска процесса, а не читается после Dispose()). НИЧЕГО не меняй, не коммить и не пушь. Просто выполни `git status --short` и `git branch --show-current` в текущей директории и выведи результат в стандартный вывод."
}
